<h1>投稿一覧</h1>

<%= link_to "新規投稿" , new_post_path %>

<% @posts.each do |post| %>
  <div>
    <h2><%= post.title %></h2>

    <p>投稿者：<%= post.user.name %></p>
    <p>評価：<%= post.rating %> / 5</p>

    <%= link_to "詳細を見る", post_path(post) %>
  </div>
<% end %>
<h2>投稿一覧</h2>

<% @posts.each do |post| %>
  <div>
    <p>
      <%= link_to post.title, admin_post_path(post) %>
    </p>
    <p>投稿者：<%= post.user.name %></p>
    <p>評価：<%= post.rating %></p>
    <p>コメント数：<%= post.comments.count %></p>
  </div>
<% end %>